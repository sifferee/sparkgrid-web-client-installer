# AGENTS.md — общая память между Claude / Qwen / Hermes

Этот файл — не документация проекта, а мост между инструментами. У каждого
ИИ, с которым ты работаешь, контекст обнуляется между сессиями и не виден
другим инструментам. Единственное, что видно всем — то, что лежит в
репозитории. Этот файл — часть репозитория специально для того, чтобы
следующая сессия (любым инструментом) не начинала с нуля.

ПРАВИЛО: открывай (или проси инструмент открыть) этот файл в начале КАЖДОЙ
сессии. Перед тем как закончить сессию — обнови секцию "Последнее изменение"
и, если нужно, "Известные баги".

## Роли

- **Hermes** — единственный инструмент, который пишет и коммитит код
  напрямую в репозиторий. Если Claude или Qwen предлагают патч — он идёт
  через Hermes, а не применяется руками параллельно.
- **Claude** — диагностика сложных багов, ревью, архитектурные решения,
  готовые промпты/патчи для Hermes.
- **Qwen** — [впиши, для чего используешь конкретно]

## Золотые правила

1. Только один инструмент пишет в репозиторий в один момент времени. Не
   гонять два агента параллельно на одних и тех же файлах.
2. Перед началом сессии — `git log -5 --oneline`, посмотреть что изменилось
   с прошлого раза и каким инструментом (см. секцию ниже).
3. Коммит после каждого осмысленного изменения, с сообщением "что и почему",
   не "fix"/"wip"/"update".
4. Любой инструмент, закончив сессию с реальными изменениями в коде,
   обновляет секцию "Последнее изменение" ниже — дата, что сделано, каким
   инструментом, номер коммита.

## Последнее изменение

- 2026-08-11 — Claude (диагностика) + Hermes (применение, коммит 48f136c) —
  vision fallback для make_public: если кнопка "Switch to public" в
  диалоге подтверждения не находится обычными селекторами (4 из 10
  сбоев в диагностике) — теперь пробуется зрение как последний резерв.
  Зрение НЕ подключено к поиску тумблера приватности (в начале функции)
  — сознательно, ошибочный клик мог бы переключить уже публичный
  аккаунт обратно в приватный. Плюс `attempt_vision_click()` вынесен в
  blocking_popup_transaction.py как переиспользуемый примитив (без
  привязки к consent-действиям), консент-специфичная функция стала
  обёрткой над ним. Регрессия: pytest 6 passed.
- 2026-08-11 — Claude (диагностика) + Hermes (применение, коммит d3781e9) —
  фикс: попап "Turn on Notifications" теперь дожимается кликом "Not now"
  вместо того чтобы оставаться нетронутым. JS-инспектор уже правильно
  распознавал категорию и знал действие (dismiss_not_now), но Python-код
  считал notifications_prompt "terminal" и уходил без клика. Теперь перед
  terminal-выходом делается одна попытка клика; если получилось —
  цепочка продолжается; если нет — старое поведение (не зависает).
- 2026-08-11 — Claude (диагностика) + Hermes (применение, коммит 82ffec7) —
  подключение vision-fallback к реальному консент-флоу: в двух местах
  где консент раньше сдавался после исчерпания попыток структурного/
  текстового поиска (cookie_consent ветка в resolve_typed_consent_chain
  и resolve_regional_ads_consent для ads-шагов) — теперь вызывается
  `vision_fallback.click_via_vision()`. Зрение включается ТОЛЬКО при
  reason == "action_unavailable" или "container_missing" (честное «не
  нашёл»); при "interaction_failed" (реальное исключение) НЕ вызывается,
  чтобы не маскировать баги в коде. Недоступность модуля зрения не роняет
  функцию. py_compile OK, pytest 6 passed.
- 2026-08-11 — Claude (диагностика) + Hermes (применение, коммит 1899dc8) —
  фикс telegram_bot.py и view_analytics.py: (1) crash-loop guard в
  `run_forever()` — 5 быстрых падений подряд (<30с) → явное сообщение в
  лог и остановка, вместо вечного цикла; (2) атомарная запись файла
  пользователей через `os.replace` (временный файл + rename), защита от
  порчи при форсированном убийстве процесса; (3) логирование во все
  `except Exception` (12 мест в telegram_bot.py, 8 в view_analytics.py).
  Применено через Hermes: git pull → замена 2 файлов → py_compile OK →
  commit → push → перезапуск бота через stop_bot.ps1/start_bot.ps1.
- 2026-08-10 — Claude (диагностика) + Hermes (применение, коммит bad984c) —
  фикс гонки на /consent/: `recover_initial_browser_load` считал страницу
  готовой сразу при `document_category == "instagram_document"` (true как
  только у body есть хоть один дочерний элемент, включая пустой
  pre-mount React root). Добавлен сигнал `rendered_text_length` в
  `_DOCUMENT_SCRIPT` и bounded-wait (до 5с) — ждём либо recognized
  surface, либо непустой текст, прежде чем доверять голой категории.
  Диагностика: katrinat82h/stephanie6xyq получали 0 байт текста на
  холодном профиле через прокси (JS-бандл не успевал догрузиться до
  проверки); sushilarangarajan012 прошёл (бандл уже был закеширован).
- 2026-08-10 — Claude (диагностика) + Hermes (применение, коммит d5c80d4) —
  фикс `_ACTION_SCRIPT` (дубль `const typedCookie`, висячая кавычка, `rx`
  объявлен до использования) + логирование во все `except Exception` в
  blocking_popup_transaction.py / instagram_consent_flow.py /
  instagram_web_profile_workflow.py + новый tests/test_js_syntax.py
  (авто-находит все `_XXX_SCRIPT` константы под patches/ и гоняет
  `node --check`). Применено через Hermes: git pull → замена 5 файлов →
  py_compile OK → pytest 6 passed → commit → push.

## Известные баги / в работе

- Ads-consent цепочка (Get started → Agree) с новым `_ACTION_SCRIPT` не
  тестирована на живых аккаунтах, только cookie_allow_all. (частично
  подтверждено: sushilarangarajan012 прошёл весь wizard cookie→ads→7
  шагов → logged_in; katrinat82h/stephanie6xyq не дошли до клика из-за
  гонки рендеринга, теперь должен быть зафиксирован).
- `human.click()` в perform_fresh_action не проверялся отдельно — элемент
  теперь находится корректно, но клик по нему в реальном Camoufox не
  верифицирован. (подтверждён клик по cookie_allow_all и всем ads-шагам
  на sushilarangarajan012, success=True на каждом шаге).

## Не трогать без причины

- (сюда вписывать код, который выглядит странно, но реально нужен — чтобы
  очередной агент не "улучшал" то, что не просили)
